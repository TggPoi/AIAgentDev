✅ **方案 C 已完成，以后启动零等待、不联网、不解析。**

## 完成的工作

1. **本地安装**：在 `D:\AI_Agent_Project\Dsh-start` 执行 `npm install @deepseek-ai/dsh`，装入了 `node_modules`（530 个包，版本 `0.1.0-rc.6`，与你现在运行的完全一致）
2. **验证通过**：`node node_modules\@deepseek-ai\dsh\lib\bin.js --version` → `0.1.0-rc.6` ✓，离线可运行 ✓
3. **更新脚本**：[start-dsh.bat](D:\AI_Agent_Project\Dsh-start\start-dsh.bat) 已改为直接调用本地安装（先 `cd /d "%~dp0"` 切到脚本目录，再 `node "node_modules\@deepseek-ai\dsh\lib\bin.js" web`）——**完全没有 npx、没有 registry 解析、没有网络依赖**

## 以后怎么启动

- **双击** `D:\AI_Agent_Project\Dsh-start\start-dsh.bat` → 秒启动，浏览器开 http://127.0.0.1:3080
- 或在任意终端先 `cd D:\AI_Agent_Project\Dsh-start`，再运行：
  - `npx dsh web`（npx 会命中本地 `node_modules\.bin\dsh`，同样不查 registry）
  - 或直接 `node node_modules\@deepseek-ai\dsh\lib\bin.js web`

## 几点说明

| 事项 | 说明 |
|---|---|
| **断网也能启动** | 本地安装完全不依赖网络 ✓ |
| **更新 dsh** | 以后想升级：在该目录执行 `npm install @deepseek-ai/dsh@latest` |
| **卸载** | 删掉 `D:\AI_Agent_Project\Dsh-start\node_modules`（或在该目录 `npm uninstall @deepseek-ai/dsh`）即可，不碰系统全局环境 |
| **之前的 npx 缓存** | 已不再用于启动，留着无影响（npm 自动管理） |
| **主题不受影响** | 皮肤在 profile 层（`C:\Users\TGG\.dsh\profiles\web`），与启动方式无关，每次启动自动生效 |