# 会话交接与上下文快照（压缩用）

> 用途：本会话在上下文 70% 占用时进行压缩交接。新会话（或新 Agent）先读本文件 + `python-agent-study/AGENTS.md`，即可无损失接续。
> 生成时间：2026-08（问题十二实施与验收全部完成之后）。

---

## 一、本会话已完成并验证的事项（全部在磁盘上，可放心丢弃对话记忆）

### 1. 问题十二一致性改造：实现 + 五层验收 ✅
- 结论：GPT 方案经审查发现 **5 个上线即失败缺陷**，已在修订版修复并全部落地；12 步实施完成。
- 代码变更：修改 20 个文件、新增 14 个文件（含 11 个代码/脚本）。
- 验收证据（真实执行）：
  - PG 原子回归 8 场景 `test_agent_task_plan_postgres.py` → passed（含修订语义：幂等复活×3、waiting_confirmation→completed、容量 429）；
  - 多进程互斥 `test_agent_task_plan_multiprocess.py` → `acquired=1 + AGENT_TASK_PLAN_BUSY=1`；
  - Store+租约冒烟 `test_agent_task_plan_store_smoke.py` → passed；
  - 现有 checkpoint 回归 `test_deep_document_checkpoint_runtime.py` → passed（已适配新夹具）；
  - 4 Worker HTTP 争抢 `accept_agent_task_plan_http_contention.py` → `409×19 + 唯一执行者`，DB 核对 1 failed + 2 rejected 命令、fence 只 +1；
  - 10 人压测冒烟 `accept_agent_task_plan_load.py` → 14/14 成功、0 个 5xx、10/10 用户参与（报告 `reports/agent-task-plan-load-smoke.json`）。
- 数据库：Alembic head = `20260813_0013`（已应用到本地 PG）。旧 JSON 导入 dry-run 校验 34 个快照（未 `--apply`）。

### 2. 文档产出（`scripts/docs/` 下）
| 文件 | 用途 |
|---|---|
| `问题十二-Agent TaskPlan多Worker一致性完整解决方案-审查修订版.md` | 修订后的权威实施方案（含 5 修复 + 实施补充⑩⑪⑫） |
| `问题十二-Agent TaskPlan多Worker一致性方案-学习页.html` | 11 标签页学习页（含真实行号代码对照 + 验收证据），浏览器已可开 |
| `问题十二-验收量化报告-简历面试用.md` | 简历/面试量化报告（含诚实边界） |
| `问题十二-压力测试方案-含时间与Token成本估算.md` | P0–P5 压测方案，总量约 10–20M tokens |
| `问题十二-部署方案.md` | 单机→企业多实例部署（网关 SSE、PgBouncer、AES 一致性等） |
| `问题十二-320秒延迟根因分析报告.md` | 320s 根因（模型吞吐+串行阶段），含 git 取证与恢复快照 |

### 3. 关键环境状态
- Docker 全栈健康：pg_vector_db(5432) / agent_redis(6379) / es-dev(9200) / milvus-standalone(19530，含 etcd+minio)。
- `.env`：模型全为 DashScope qwen（LLM=qwen3.7-plus；Router/Guard=qwen3.6-flash；Reviewer=qwen3.7-max；rerank=qwen3-rerank）。
- `_recovered/`：从 git 对象库恢复的 320s 验收任务快照（JSON+MD，task_plan_20260726175005_8acd72b40d07）——用户尚未决定保留或删除。
- 8000 端口当前空闲（验收用服务器已关闭）。

---

## 二、待用户决策事项（新会话的第一优先级，附推荐默认值）

> 【已定稿，2026-08 压缩前确认】压测形态：**《问题十二-压测方案-30元精简版.md》的 P0' → P1 → P2'' 混合负载（15 RAG + 2 Research + 1 Document + 2×429 撞槽，同一时间窗口并发执行）**；预算 ≤30 元；机器规格已采集（6C12T/32GB/Win10，简历不写型号、报告标注"测试执行环境"）；`.env` 模型矩阵已更新（辅助链路统一 qwen3.7-flash）。**执行尚未开始**——新会话必须先向用户确认"开始执行"再动手。

| # | 决策 | 状态 | **推荐（可照单采纳）** |
|---|---|---|---|
| 1 | **复杂任务 SLO 定多少** | 待定 | 首版验收：单任务 ≤480s 且零超时撞线；320s 记为性能基线；优化目标另立 ≤300s |
| 2 | **压测费用上限** | ✅ 已定 ≤30 元 | 用《30元精简版》方案（设计用量 ≈1.1M、预估 6–10 元）；完整版备用 |
| 3 | **15 个真实用户 JWT** | 待定 | 最优：15 个真实 JWT；不便则 8 真实 + 7 临时 `load_test_*` 账号（测后删） |
| 4 | **压测机器规格** | ✅ 已采集 | 6C12T/32GB/Win10 测试机（报告附录写"测试执行环境，非生产规格"） |
| 5 | **模型降配实验** | 待定 | P0' 内已含 5 道 qwen3.6-plus 同题对照（只存数据，不做决策） |
| 6 | **低风险优化** | 待用户点头 | 压测前可做：Researcher 超时 120→150、分阶段耗时日志（用户此前说"暂时不用动"） |
| 7 | `_recovered/` 目录 | 待定 | 保留至 P0' 基线对照完成后删除 |
| 8 | 套餐 vs 余额 | 待定 | 先按量跑 P0'+P1，校准后精算再定 |

---

## 三、下一步执行清单（定稿，按顺序）

> 预算 30 元，执行一律按《问题十二-压测方案-30元精简版.md》。**第一步永远是先向用户确认"开始执行"。**

1. （可选，待用户同意）落地 #6 两项低风险优化（Researcher 超时 150s + 分阶段耗时日志）并跑四套回归；
2. 准备测试身份（#3）+ 单价填表（方案 §2 表格）→ 启动服务（`--workers 4`，8000 端口）；
3. 执行 **P0' 校准批**（10 RAG + 1 小 Research + 5 道对照题）→ 从 DashScope 用量页核对各模型累计 token，回填预算表；
4. 执行 **P1 控制面**（0 元：20 并发争抢、cancel 重放 ×50、GET ×200）；
5. 执行 **P2'' 混合负载**（定稿形态：15 RAG 并发 3 + 2 Research + 1 降配 Document + 2×429 撞槽，槽位保持 2/1）；
6. （余额允许）P4' 10 分钟长稳——默认跳过；
7. 触顶规则：任一模型 80% 告警、100% 或累计 24 元停；5xx 即停；
8. 产出《30 元精简压测报告》：混合负载断言（0 失败/0 个 5xx/429 精确）、每请求 token 成本、复杂任务基线复现；回填完整版方案估算列。

---

## 四、给新会话的环境注意事项（本会话踩过的坑）

- **导出目录**：`AgentTaskPlanExportStore` 写 `runtime/agent-task-plans`；历史上某些执行沙箱对该目录只读（`NamedTemporaryFile` 会表现为挂起而非报错）。跑测试时若遇挂起，设 `AGENT_TASK_PLAN_DIR` 到可写目录；普通本机运行无此问题。
- **MCP stdio**：`.env` 里 MCP fetch 的 command 是 Windows venv 路径；容器化要改写（部署方案 §5.4）。
- **LangSmith**：`.env` 有 `LANGSMITH_API_KEY`（lsv2 开头）与项目名 `python-agent-study-phase-final-3`；历史慢 trace 在 phase-final-2（320s 级在 07-26/27，Research 慢样本 08-02~04）。用 SDK `read_run/list_runs`（注意：旧 SDK 方法名是 `read_run`，list 单页 ≤100）。
- **git 取证**：被删的 runtime 快照可用 `git rev-list --all --objects | Select-String <id>` 找 blob，`git cat-file -p <blob>` 恢复（本次已恢复 1 份）。
- **测试命令通用前缀**：`$env:PYTHONPATH="src;scripts\tests\document_security"`；文档安全测试在 `scripts/tests/document_security/`。
- **一致性改造的 4 个新错误码**：`AGENT_TASK_PLAN_BUSY / LEASE_LOST / VERSION_CONFLICT / IDEMPOTENCY_CONFLICT` + 429 `AGENT_CAPACITY_EXCEEDED`（带 Retry-After）——监控告警依据（部署方案 §10）。

---

## 五、会话结论一句话

问题十二已完成：方案审查 → 实现 → 五层验收 → 文档化全部闭环。压测方案已按 30 元预算定稿（P0'→P1→P2'' 混合负载，形态已由用户确认），**执行尚未开始**；新会话读本文件 +《30元精简版》即可接续，先向用户确认再动手。
