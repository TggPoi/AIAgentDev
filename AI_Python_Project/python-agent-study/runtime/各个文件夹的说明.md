结论先说：这三个目录虽然都位于 `runtime`，但性质完全不同。

| 目录 | 性质 | 是否由正常导入程序产生 |
|---|---|---|
| `runtime/knowledge-imports` | Office 文件上传暂存区 | 是 |
| `runtime/office-acceptance` | PPT/Excel 真实验收的备份和证据 | 否，是验收过程产生 |
| `runtime/real-acceptance` | 较早一次 RAG/Agent 真实链路验收的 Uvicorn 日志 | 否，是验收启动脚本产生 |

## 一、`runtime/knowledge-imports`

这是正式 Office 导入功能使用的临时文件目录。

### 里面保存的是什么

用户上传 PPTX/XLSX 后，API 不会马上覆盖知识库里的正式文件，而是先复制到这里：

```text
用户上传 assets.xlsx
    ↓
runtime/knowledge-imports/import_xxx.xlsx
```

文件名使用导入任务的 `job_id`，避免两个用户上传同名文件时发生冲突。

目录定义在：

[import_jobs.py](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/ingestion/import_jobs.py:26)

```python
STAGING_DIR = Path("runtime/knowledge-imports")
```

API 创建暂存文件的位置在：

[knowledge_import_routes.py](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/api/knowledge_import_routes.py:379)

主要执行过程是：

1. 创建 `runtime/knowledge-imports`。
2. 生成 `import_<uuid>.pptx/xlsx` 文件名。
3. 分块读取 `UploadFile` 并写入该文件。
4. 同时计算文件 SHA-256。
5. 对暂存文件执行 OOXML ZIP 安全校验。
6. 把暂存路径写进 PostgreSQL 任务记录。

对应代码：

- 生成暂存路径：[knowledge_import_routes.py:379](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/api/knowledge_import_routes.py:379)
- 复制上传内容：[knowledge_import_routes.py:384](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/api/knowledge_import_routes.py:384)
- OOXML 校验：[knowledge_import_routes.py:388](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/api/knowledge_import_routes.py:388)

### 哪个程序阶段产生

它产生于：

```text
POST 上传请求
→ 权限和目标路径检查
→ 创建 job_id
→ 将上传文件写入 knowledge-imports     ← 在这里产生
→ OOXML 校验
→ PostgreSQL 创建 pending 任务
→ API 返回 202
```

也就是说，它是在 Worker 领取任务之前产生的。

### Worker 如何使用它

Worker 领取任务后，始终读取这里的暂存文件：

```text
validating
    检查文件存在、SHA-256、OOXML
extracting
    PowerPointDocumentLoader / ExcelDocumentLoader 解析
chunking
    PowerPointChunkBuilder / ExcelChunkBuilder 分块
loading_existing_chunks
diffing
embedding
indexing
verifying
publishing
    将暂存文件原子发布到正式知识库路径
completed
    删除暂存文件
```

完整处理主线可从这里看到：

[worker.py](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/ingestion/worker.py:275)

成功发布后删除暂存文件：

[worker.py:418](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/src/fast_app/ingestion/worker.py:418)

因此，在系统没有活动导入任务时，这个目录理论上应当为空。

### 当前两个 XLSX 文件是什么

当前目录有两个文件：

```text
import_1374b1f2e209409db495b5a89fff8c93.xlsx
import_46b4218f7e8f4927af286d1aebf93801.xlsx
```

我实际检查后确认：

- 两个文件完全相同。
- 大小都是 5835 字节。
- SHA-256 都是：

```text
2ce94c9d8ca3ccd78022f1131ddb9fd97cc3c61ad75acd01ebaa6e7859635ed2
```

- 内容是自动回归使用的测试工作簿：

```text
Assets Sheet
ID | Name   | Cost|CNY | 公式
1  | Server | 10       | =A2+1

Hidden Sheet
secret
```

它与 [test_office_ingestion.py:139](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/scripts/test_office_ingestion.py:139) 中的 `build_xlsx()` 测试夹具一致。

我还查询了当前 PostgreSQL，已经没有这两个 `job_id` 对应的任务记录。因此它们不是仍在运行或等待配置的有效导入任务，而是自动化测试中途退出或异常结束后留下的测试暂存文件。正常完成测试时，脚本会在这里清理：

[test_office_ingestion.py:1023](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/scripts/test_office_ingestion.py:1023)

## 二、`runtime/office-acceptance`

这是我们在 2026-07-15 执行 PPT、Excel、Markdown 真实导入验收时建立的证据目录。

它不是 Worker 的业务目录，正常用户上传文件不会在这里产生内容。

对应的验收记录在：

[PPT和Excel文件格式测试报告.md](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/scripts/docs/PPT和Excel文件格式测试报告.md:1)

运行目录：

```text
runtime/office-acceptance/20260715-230846/
```

其中 `20260715-230846` 是这次验收的运行编号。

### `originals`

保存测试文档修改前的原始版本：

```text
originals/
├─ UE5战斗系统程序架构设计_RAG测试.md
├─ UE5战斗系统设计方案_RAG测试用PPT.pptx
└─ 游戏开发资产列表_RAG测试.xlsx
```

产生阶段：

```text
开始真实验收
→ 计算原文件 SHA-256
→ 将三份原始文件复制到 originals     ← 在这里产生
→ 后续才修改工作区中的测试文件
```

用途是：

- 人工比较修改前后内容。
- 验证 SHA-256。
- 发生测试失败时恢复原始文件。
- 验证 PPT 插页、Excel 插行插列、Markdown 新增章节的增量行为。

### `modified-candidates`

当前保存了一份：

```text
modified-candidates/游戏开发资产列表_RAG测试.xlsx
```

它是验收期间生成的“修改候选文件”。

产生阶段：

```text
加载原始 Excel
→ 插入测试资产行
→ 插入“审核人”列
→ 保存为 modified-candidates 中的候选文件
→ 检查文件结构和 SHA
→ 再通过更新 API 上传
```

这里的文件不是 Worker 解析后的产物，而是“准备交给更新 API 的修改版源文件”。

### `temporarily-removed` 和 `temporarily-removed-2`

这些目录保存验收期间临时从知识库目标路径移走的 PPTX/XLSX。

原因是基础创建验收需要覆盖真实的创建接口：

```text
知识库目标路径已经存在原始 PPTX/XLSX
    ↓
如果直接调用创建 API，会因为目标文件已存在而返回 409
    ↓
先把文件移到 temporarily-removed
    ↓
再调用 POST /knowledge-documents/import-jobs
    ↓
验证真正的上传、暂存、Worker、发布链路
```

因此它们产生于“基础创建导入验收准备阶段”，不是 Loader、Builder 或 Worker 自动创建的目录。

`temporarily-removed-2` 是验收中第二轮临时转移留下的证据目录。

### `fastapi.stdout.log` 和 `fastapi.stderr.log`

这是该次验收启动 FastAPI 服务时重定向的标准输出和错误输出。

当前两个文件为空，说明对应服务进程没有向这些文件写入内容，或者真正的日志由其他日志处理器输出。

### 为什么验收完成后还保留

这是测试计划明确要求保留的：

- 工作区保留修改后的三份测试文档。
- `originals` 保留修改前版本。
- 报告记录修改前后 SHA-256。
- 便于你人工复查。

所以 `office-acceptance` 属于有意保留的验收证据，不是垃圾缓存。


这个目录与 Office 导入功能无关。

它是 2026-07-13 进行 RAG、Agent、权限、任务确认、重试和 Prompt Guard 真实验收时留下的 Uvicorn 日志。

当前包含三组服务日志。

### `uvicorn.stdout.log` / `uvicorn.stderr.log`

这是默认真实验收服务，运行在：

```text
http://127.0.0.1:8000
```

启动证据：

[uvicorn.stderr.log](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/runtime/real-acceptance/uvicorn.stderr.log:1)

这组日志记录了：

- `/health`
- `/auth/login`
- `/auth/me`
- `/rag/chat`
- `/rag/chat/stream/events`
- 文档创建、修改、删除
- Agent TaskPlan 生成
- TaskPlan confirm/cancel
- ES/Milvus 检索
- Redis/PostgreSQL 对话记忆
- Prompt Guard 拦截

其中：

- `stdout.log` 主要是 HTTP Access Log，例如请求路径和状态码。
- `stderr.log` 主要是应用结构化日志、警告和异常堆栈。

### `uvicorn-guard-off.stdout.log` / `uvicorn-guard-off.stderr.log`

这是关闭 Prompt Guard 后的对照验收服务，运行在：

```text
http://127.0.0.1:8001
```

启动证据：

[uvicorn-guard-off.stderr.log](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/runtime/real-acceptance/uvicorn-guard-off.stderr.log:1)

它用于验证：

```text
默认服务：
删除文档请求
→ Prompt Guard 可能直接阻止

guard-off 服务：
同一个请求
→ 跳过 Prompt Guard
→ 继续进入 Router、TaskPlan、权限检查和人工确认
```

这不是生产运行产生的特殊日志，而是为了区分“Prompt Guard 阻止”和“后续权限/任务流程阻止”而专门启动的对照服务。

### `uvicorn-max1.stdout.log` / `uvicorn-max1.stderr.log`

这是把文档 Agent 最大工具调用次数限制为 1 的故障验收服务，运行在：

```text
http://127.0.0.1:8002
```

启动证据：

[uvicorn-max1.stderr.log](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/runtime/real-acceptance/uvicorn-max1.stderr.log:1)

该场景故意让文档修改任务达到工具调用上限，并验证：

```text
第一次执行
→ 达到最大工具调用次数
→ /rag/chat 返回 400
→ TaskPlan 保留失败状态
→ 之后调用 retry
→ 再次 confirm
→ 验证任务恢复能力
```

日志中的明确错误是“文档 Agent 已达到最大工具调用次数”：

[uvicorn-max1.stderr.log:101](D:/AI_Agent_Project/AI_Python_Project/python-agent-study/runtime/real-acceptance/uvicorn-max1.stderr.log:101)

### 它在哪个阶段产生

它不是业务代码在某个 Worker 阶段自动生成的，而是在真实验收时通过类似下面的方式启动服务产生的：

```powershell
uvicorn fast_app.main:app `
  --port 8000 `
  1> runtime\real-acceptance\uvicorn.stdout.log `
  2> runtime\real-acceptance\uvicorn.stderr.log
```

因此它对应的是整套 RAG/Agent 接口验收，不对应 `validating`、`chunking` 或 `embedding` 等 Office Worker 阶段。

## 生命周期总结

```text
正常 Office 上传
│
├─ knowledge-imports
│   ├─ API 上传时创建暂存文件
│   ├─ Worker 全流程读取
│   ├─ publishing 时发布到正式路径
│   └─ succeeded 后删除
│
Office 真实验收
│
└─ office-acceptance
    ├─ originals：修改前备份
    ├─ temporarily-removed：基础创建测试前临时移走
    ├─ modified-candidates：修改场景候选文件
    └─ 日志：验收服务输出
       
```

所以，只有 `knowledge-imports` 是应用正式运行依赖的临时目录；另外两个目录都是为人工复查而保留的测试证据。